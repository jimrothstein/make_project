## Goal:  reorganize certain make files (*.mk*) in this directory.


## Some files may be combined
- Files  beginning with 0030_, 0020, 0110 and rename to 0110_linux_shell_cmds.mk
- Files beginning with 0060_, 0065_, 0090_regex  .  Rename resulting file to 0210_find_grep_regex.md

## If file is not listed above, ignore it.

## Do not edit the files other than appending.  If you see make errors, ignore these.

## This will be a "project" and therefore needs "project management file".  Commit before any changes are made.  Commit and push only when user approves.







